#include<stdio.h>
void main()
{
  long int rem[50],num,i=0,length=0,n;
    printf("enter the decimal number");
    scanf("%ld",&num);
    while(num>0)
    {
        rem[i]=num%2;
        num=num/2;
        i++;
        length++;
    }
     printf("\n the binary number is\n");
    for(i=length-1;i>=0;i--)
   {
     printf("%ld",rem[i]);
    }
}